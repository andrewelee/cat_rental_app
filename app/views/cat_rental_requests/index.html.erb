<ul>
  <% @rental_requests.each do |request| %>
    <li><a href="<%= cat_rental_request_url(request) %>">
      <%= Cat.find(request.cat_id).name %> : <%= request.start_date%> :

    </a> <%= request.status%></li>

  <%end%>
</ul>