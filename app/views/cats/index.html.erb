<h1>Our Cats!</h1>

<ul>
  <% @cats.each do |cat| %>
    <li><a href="<%= cat_url(cat) %>"><%=cat.name%></a></li>

  <%end%>
</ul>

<%= button_to "Request a cat!", new_cat_rental_request_url, method: :get %>